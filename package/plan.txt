takeCSV=True

# phase 2 after hackathon
if takeCSV:
    choices=["analyse1student","wholegroup"]
    choose=choice[int(input("choose a mode"))]
    if choose==choices[0]:
        if "Exam_Score_isnull":
            return["predicted_score","5featurespiderchart","position_on_persona_model","position_on_academic_model"]
        else:
            return["predicted_score","5featurespiderchart","position_on_persona_model","position_on_academic_model"]
    elif choose==choices[1]:
        if "Exam_Score_isnull":
            return["predicted_score_range_piechart","persona_wise_clustering","academic_clustering","academic_cluster_led_persona_clustering"]


# phase1 before hackathon
else:
    choices=["analyse1student","wholegroup"]
    choose=choice[int(input("choose a mode"))]
    if choose==choices[0]:
        if "Exam_Score_isnull":
            return["predicted_score","5featurespiderchart"]
        else:
            return["5featurespiderchart"]

    elif choose==choices[1]:
        if "Exam_Score_isnull":
            return["predicted_score_range_piechart","persona_wise_clustering","academic_clustering","academic_cluster_led_persona_clustering"]

# executing plan 1
for single student:
input a dict, scale it ,predict_score(if so),featureReduction
return the coordinates after feature reduction and (if there) predicted_Score ,(color_coded)
for group:
input a csv file(we ask user to upload a csv)
we scale it,predict_score(if so),runPredictionUsing3ClusterModel,now for each of these clusters